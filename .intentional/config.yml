$schema: https://intentional.foo/schemas/config.yml
contract: contract-1
settings:
  internal-dependency-bump: patch
  pre-1-0-bump-mapping: component
discovery:
  managed-paths:
  - detector: npm-package
    path: package.json
    release-unit: t3code-client
release-units:
  t3code-client:
    path: .
    projections:
    - adapter: npm
      file: package.json
      mode: committed
    tags:
      primary:
        role: primary
        template: '{id}@{version}'
